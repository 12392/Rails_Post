$('#descriptionid').val('')
$( "#display" ).prepend("<%= j render 'comments/comment' %>");









<%# 
$('#create').click(function(e){
  e.preventDefault();
  alert("click me")
   %>
  <%# $.ajax({
    url:'/comments',
    type: 'post',
    data: {
            "id":@comment.id,
            "comment":@comment.comment,
            "post_id":@comment.post_id
          },
    success:function(data){
      alert(data)
    }
    });
    alert(data) %>

<%# }); %>




 <%# $(document).ready(function(){
   alert("page has loaded!");

   $('#create').click(function(e){
  
  alert("comment created by js.erb in document");
  e.preventDefault();
        });
}); %>

<%# $('#AddComment').on('Submit',function(e){
  
  alert("comment created by js.erb in document");
  e.preventDefault();
        
}); %>
